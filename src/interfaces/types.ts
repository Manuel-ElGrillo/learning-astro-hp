export interface MenuItem {
    title: string,
    url: string
}

export interface CardItem {
    title: string;
    image: string;
    description: string;
  }
  
  export interface faqs {
    question: string;
    response: string;
  }